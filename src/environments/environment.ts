export const environment = {
  production: false,
  // If you want to use GitHub GraphQL for contributions, add a personal access token here.
  // Example scopes: public_repo (for public contributions). IMPORTANT: do NOT commit real tokens to public repos.
  githubToken: '' // e.g. 'ghp_xxx' (leave empty to use mock data / svg fallback)
};
